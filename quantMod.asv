function [qR qG qB] = quantMod(imgStore)
%{
The purpose of this function is to carry out the image quantization. We
will take an input image and divide it into each R G B component and then
compare each elements. When the comparison is done, we can take
quantization level for each elements. 

%}
imgHold = imgStore;
end
